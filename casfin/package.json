{
  "name": "casfin",
  "version": "0.1.0",
  "private": true,
  "description": "CasFin is a transparent onchain prediction market and casino protocol for Arbitrum.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:casino": "hardhat run scripts/deployCasinoStage.ts --network arbitrumSepolia",
    "deploy:fhenix": "hardhat run scripts/deployFhenix.ts --network fhenixHelium",
    "deploy:prediction": "hardhat run scripts/deployPredictionStage.ts --network arbitrumSepolia",
    "deploy:all": "hardhat run scripts/deployAllStages.ts --network arbitrumSepolia",
    "verify": "hardhat verify --network arbitrumSepolia",
    "keeper:start": "tsx keeper/index.ts",
    "keeper:start:fhe": "tsx keeper/fhe-keeper.ts",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:start": "npm --prefix frontend run start"
  },
  "devDependencies": {
    "@fhenixprotocol/cofhe-contracts": "^0.1.3",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@nomicfoundation/hardhat-verify": "^2.0.12",
    "@types/node": "^24.5.2",
    "chai": "^4.5.0",
    "dotenv": "^16.5.0",
    "ethers": "^6.13.5",
    "hardhat": "^2.24.2",
    "hardhat-abi-exporter": "^2.11.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
